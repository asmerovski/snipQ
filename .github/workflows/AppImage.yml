name: AppImage

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-appimage:
    name: Build Linux AppImage (Ubuntu 22.04)
    runs-on: ubuntu-22.04

    steps:
      # ── Checkout ────────────────────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4

      # ── Dependencies ────────────────────────────────────────────────────────
      - name: Cache apt packages
        uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: >
            build-essential
            cmake
            ninja-build
            qt6-base-dev
            qt6-base-dev-tools
            libqt6sql6-sqlite
            libgl-dev
            libxkbcommon-dev
            libvulkan-dev
            libfuse2
          version: 1.0

      # ── Build ───────────────────────────────────────────────────────────────
      - name: Configure
        run: |
          cmake -B build -S . \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/usr

      - name: Build
        run: cmake --build build --parallel $(nproc)

      # ── Stage AppDir ────────────────────────────────────────────────────────
      - name: Install into AppDir
        run: DESTDIR=${{ github.workspace }}/AppDir cmake --install build

      - name: Write desktop entry
        run: |
          mkdir -p AppDir/usr/share/applications
          cat > AppDir/usr/share/applications/snipQ.desktop << 'DESKTOP'
          [Desktop Entry]
          Type=Application
          Name=snipQ
          Comment=Cross-platform code snippet manager
          Exec=snipQ
          Icon=snipQ
          Categories=Development;Utility;
          Keywords=snippet;code;clipboard;
          DESKTOP

      - name: Copy icon
        run: |
          mkdir -p AppDir/usr/share/icons/hicolor/256x256/apps
          cp resources/app.png AppDir/usr/share/icons/hicolor/256x256/apps/snipQ.png
          mkdir -p AppDir/usr/share/icons/hicolor/scalable/apps
          cp resources/app.svg AppDir/usr/share/icons/hicolor/scalable/apps/snipQ.svg

      # ── Package AppImage ────────────────────────────────────────────────────
      - name: Download linuxdeploy + Qt plugin
        run: |
          wget -q https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          wget -q https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          chmod +x linuxdeploy-x86_64.AppImage linuxdeploy-plugin-qt-x86_64.AppImage

      - name: Build AppImage
        env:
          QMAKE: /usr/lib/qt6/bin/qmake
          OUTPUT: snipQ-linux-x86_64.AppImage
        run: |
          ./linuxdeploy-x86_64.AppImage \
            --appdir AppDir \
            --plugin qt \
            --output appimage \
            --desktop-file AppDir/usr/share/applications/snipQ.desktop \
            --icon-file AppDir/usr/share/icons/hicolor/256x256/apps/snipQ.png

      # ── Artifact / Release ──────────────────────────────────────────────────
      - name: Upload AppImage artifact
        uses: actions/upload-artifact@v4
        with:
          name: snipQ-linux-x86_64
          path: snipQ-linux-x86_64.AppImage
          if-no-files-found: error

      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: snipQ-linux-x86_64.AppImage
